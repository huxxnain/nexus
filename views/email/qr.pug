extends baseEmail

block content
    p Hi,
    img.card__picture-img(src=`${qr}`, alt=`qr` align='center' style="display:block; margin-left: auto;margin-right: auto;" )
    p Select add account with qr code to scan the qr code given here.
    p Click the login and Use the token generated by the app in the code section.
    p Stay Safe, Stay Blessed 
        table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
                tr
                    td(align='left')
                        table(role='presentation', border='0', cellpadding='0', cellspacing='0')
                            tbody
                                tr
                                    td
                                        a(href=`${redirect}`, target='_blank') Login
